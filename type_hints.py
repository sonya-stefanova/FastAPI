def add_integers(first_number: int, second_number: int): # Format | parameter: type (type can be int, str, bool, ...)
    return first_number+second_number# Editor support added

print_sum = add_integers('ab','cd')
print_sum1 = add_integers(1,2)
print(print_sum1)# Doesn't actually restrict anything